class Solution {
public:
    int bitwiseComplement(int n) {
        if(n==0)
        return 1;
     int i=0;
     int ans=0;
    while(n > 0) {
        int bit = n % 2;
        bit=!bit;
        ans+=pow(2,i)*bit;
        n = n /2;
        i++;
    }
return ans;
  
}
        
};
